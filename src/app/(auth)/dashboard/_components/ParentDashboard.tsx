import React from "react";
import RequirementsSection from "./RequirementSection";
import TutionSection from "./TutionSection";

const ParentDashboard = () => {
  return (
    <div>
      <RequirementsSection />
      <TutionSection />
    </div>
  );
};

export default ParentDashboard;
